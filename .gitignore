# Compiled OpenModelica artifacts (regenerated by `omc build.mos`)
BuildingOpt
BuildingOpt_*
BuildingOpt.c
BuildingOpt.o
BuildingOpt.libs
BuildingOpt.log
BuildingOpt.makefile
*.o
*.c
*.h

# fz cache / run artifacts
.fz/
__pycache__/

# Notebook working-directory artifacts (this app doesn't use them: it calls
# app_fzr/ and downloads weather dynamically instead of weather.txt)
/run.sh
/params.txt
/params_robust.txt
weather.txt

# Generated data / plots / logs (reproducible via the notebook or app)
*.csv
*.png
!screenshot.png
*.html
om.log

.DS_Store
